{
  "name": "rustpress",
  "version": "1.0.0",
  "description": "Standalone VitePress-format docs site generator in Rust",
  "scripts": {
    "test": "npm run build:js && npm run build:css && cargo test --quiet && npm run check:demo && npm run check:parity",
    "check:demo": "cargo run --quiet -- build demo",
    "build:docs": "cargo run --quiet -- build docs",
    "build:showcase": "cargo run --quiet --bin build_theme_showcase",
    "dev:docs": "npm run watch:css & npm run watch:js & cargo run -- serve docs & wait",
    "check:parity": "cargo run --quiet -- parity check demo/public",
    "parity:refresh": "cp parity/upstream.json parity/upstream.prev.json 2>/dev/null; node scripts/parity-refresh.mjs && cargo run --quiet -- parity snapshot parity/cache --meta parity/cache/meta.json && cargo run --quiet -- parity diff parity/upstream.prev.json parity/upstream.json; rc=$?; rm -f parity/upstream.prev.json; exit $rc",
    "diff:upstream": "bash scripts/diff-upstream.sh",
    "build:css": "tailwindcss -i styles/vitepress.css -o static/main.css --minify",
    "watch:css": "tailwindcss -i styles/vitepress.css -o static/main.css --watch",
    "build:js": "esbuild js/alpine-entry.js --bundle --format=iife --target=es2018 --minify --outfile=static/js/app.js",
    "watch:js": "esbuild js/alpine-entry.js --bundle --format=iife --target=es2018 --outfile=static/js/app.js --watch",
    "build": "npm run build:js && npm run build:css && cargo run --quiet -- build demo",
    "dev": "npm run watch:css & npm run watch:js & cargo run -- serve demo & wait"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/at-least/rustpress.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@alpinejs/collapse": "^3.17.2",
    "@alpinejs/intersect": "^3.17.2",
    "@tailwindcss/cli": "^4.3.3",
    "alpinejs": "^3.17.2",
    "esbuild": "^0.28.2"
  },
  "private": true
}
